#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main(void){
  int n, k;
  cin >> n >> k;

  int m[n][n];
  for(int i = 0; i < n; i++){
    for(int j = 0; j < n; j++) {
      cin >> m[i][j];
    }
  }

  int min = 1000000001;
  return 0;
}